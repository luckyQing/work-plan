<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.luckyqing.mapper.DemandMapper">

    <!-- 通用结果映射：需求信息 + 项目名称 + 创建人姓名 -->
    <resultMap id="demandWithDetailMap" type="io.github.luckyqing.entity.Demand">
        <id column="id" property="id"/>
        <result column="demand_name" property="demandName"/>
        <result column="demand_type" property="demandType"/>
        <result column="project_id" property="projectId"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="priority" property="priority"/>
        <result column="creator_id" property="creatorId"/>
        <result column="start_date" property="startDate"/>
        <result column="end_date" property="endDate"/>
        <result column="total_hours" property="totalHours"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
        <result column="project_name" property="projectName"/>
        <result column="creator_name" property="creatorName"/>
    </resultMap>

    <!-- 查询所有需求（关联项目名称、创建人姓名），按创建时间倒序 -->
    <select id="selectAllWithDetail" resultMap="demandWithDetailMap">
        SELECT d.*,
               p.project_name,
               u.real_name AS creator_name
        FROM demand d
        LEFT JOIN project p ON d.project_id = p.id
        LEFT JOIN sys_user u ON d.creator_id = u.id
        WHERE d.deleted = 0
        ORDER BY d.create_time DESC
    </select>

</mapper>
