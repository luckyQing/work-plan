<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.luckyqing.mapper.WorkLogMapper">

    <resultMap id="workLogMap" type="io.github.luckyqing.entity.WorkLogEntity">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="user_id" property="userId"/>
        <result column="log_date" property="logDate"/>
        <result column="hours" property="hours"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
        <result column="task_name" property="taskName"/>
        <result column="task_type" property="taskType"/>
    </resultMap>

    <!-- 查询指定用户指定日期的工时记录（关联任务名称） -->
    <select id="selectByUserAndDate" resultMap="workLogMap">
        SELECT w.*, t.task_name, t.task_type
        FROM t_work_log w
        LEFT JOIN t_task t ON w.task_id = t.id
        WHERE w.deleted = 0
          AND w.user_id = #{userId}
          AND w.log_date = #{logDate}
        ORDER BY w.create_time DESC
    </select>

</mapper>
